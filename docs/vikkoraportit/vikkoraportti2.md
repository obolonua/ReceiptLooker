1. We build the Synthetic Digit Generator with Pillow, OpenCV, Numpy. These are fine because they are preprocessing tools, not OCR engines. The dataset includes variations in font, size, thickness, rotation (±5°), Gaussian noise, blur, threshold artifacts, and translation to simulate different receipt fonts, scanner and printing variations, camera noise, motion or focus issues, thermal receipt textures, and segmentation imperfections.